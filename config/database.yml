development:
    adapter: postgresql
    encoding: unicode
    reconnect: false
    host: db
    database: document_storage_development
    pool: 5
    username: postgres
    password:


# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
    adapter: postgresql
    encoding: unicode
    reconnect: false
    database: document_storage_test
    pool: 5
    username: thomasboltze
    password:
    host: localhost

staging:
    adapter: postgresql
    encoding: unicode
    reconnect: false
    database: document_storage_staging
    pool: 5
    username: ruby
    password: rails
    host: localhost

production:
    adapter: postgresql
    encoding: unicode
    reconnect: false
    database: document_storage_production
    pool: 5
    username: ruby
    password: rails
    host: localhost
